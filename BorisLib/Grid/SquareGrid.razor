@using BorisLib.Grid
<CascadingValue Value="@(this)">
    @Items
</CascadingValue>

<div class="SquareGrid @Class" style="--num-columns: @Columns; @Style">
    @foreach (var gridItem in _items)
    {
        if (ItemTemplate != null)
        {
            @ItemTemplate(gridItem)
        }
        else
        {
            <div class="SquareGridItem" style="@gridItem.GridStyleString">
                @gridItem.ChildContent
            </div>
        }
    }
</div>